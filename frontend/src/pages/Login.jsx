const Login = () => {
  return (
    <section className="page">
      <h1>Login</h1>

      <p>
        Admin authentication will be connected to the
        Spring Boot backend later.
      </p>
    </section>
  );
};

export default Login;